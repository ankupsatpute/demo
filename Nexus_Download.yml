---
 - name: User
   hosts: all
   become: true
   become_user: root
   gather_facts: false
   vars:
     user_name: ansh
   tasks:
     - name: Create New User
       ansible.builtin.user:
          name: "{{ user_name }}"
